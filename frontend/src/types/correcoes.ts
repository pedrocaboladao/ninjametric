export interface SkuSemCusto {
  lojaId: number;
  lojaNome: string;
  sku: string | null;
  itemId: string;
  titulo: string;
  ocorrencias: number;
  receitaAfetada: number;
  primeiraOcorrencia: string;
  ultimaOcorrencia: string;
  motivo: string;
}
